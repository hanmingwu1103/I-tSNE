% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{standardize_interval_data}
\alias{standardize_interval_data}
\title{Standardize interval-valued data}
\usage{
standardize_interval_data(data, id_col = NULL, method = 1)
}
\arguments{
\item{data}{A data frame containing interval variables stored as paired
\verb{_Lower} and \verb{_Upper} columns.}

\item{id_col}{Optional identifier or group column to keep unchanged.}

\item{method}{One of \code{1}, \code{2}, or \code{3}, corresponding to midpoint-dispersion,
boundary-dispersion, and range standardization.}
}
\value{
A standardized data frame with the same interval-column structure as
the input.
}
\description{
Apply one of the manuscript's interval standardization schemes to a data frame
of interval-valued observations while preserving an optional identifier column.
}
\examples{
face_path <- system.file("extdata", "facedata.csv", package = "itsne")
face_raw <- readr::read_csv(face_path, show_col_types = FALSE)
face_data <- face_raw[, c("species", grep("_(Lower|Upper)$", names(face_raw), value = TRUE))]
face_std <- standardize_interval_data(face_data, id_col = "species", method = 1)
head(face_std[, 1:5])
}
